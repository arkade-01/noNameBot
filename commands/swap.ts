import { Telegraf, Context, Markup } from 'telegraf';
import getUser from '../helper_functions/getUserInfo';

const swapAction = (bot: Telegraf<Context>) => {
    bot.action('swap', async (ctx) => {
        
    })
}